import React from "react";
const Head = (props) => {
    return (
        <div className={props.class.head}>
            <div className={props.class.heading}>Debt Summary</div>
        </div>
    );
};
export default Head;
